# Внутреннее: проверка на samples

**Не для покупателя.** Эта заметка — для разработки и регрессии.

## Быстрый прогон Доктор 60/62

1. Откройте `SverkaFact _v2.1.html` (без лицензии) или PRO с ключом.
2. Режим «Доктор 60/62».
3. ОСВ: `osv-60-advance.xlsx`
4. Второй файл: `osv-76va-advance.xlsx`
5. «Аванс от» = 1000 → Проверить.

Ожидание:

- есть **ВИННЕР ДЕМО ООО**, Важность = **справочно**;
- аванс Дт ≈ 661 159,63;
- в 76.ВА контрагента нет.

Автотест парсера:

```bash
node avito/make-kb-samples.cjs
node avito/verify-severity-samples.cjs
```

## Остальные модули (ручной smoke)

| Модуль | Файлы |
|--------|--------|
| Акты | `act-a-demo.xlsx` + `act-b-demo.xlsx` |
| НДС | `sf-book-a.xlsx` + `sf-book-b.xlsx` |
| Эквайринг | `acq-terminal.xlsx` + `acq-bank.xlsx` |

После правок HTML — оба файла: `_v2.html` и `_v2.1.html`, плюс карточка в `kb/` при новой проверке.
